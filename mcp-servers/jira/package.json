{
  "name": "jira-mcp-server",
  "version": "1.0.0",
  "description": "Jira MCP Server for Model Context Protocol",
  "main": "dist/server.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node dist/server.js",
    "dev": "nodemon --exec \"npm run build && node dist/server.js\" --watch src --ext ts",
    "test": "npm run build && node dist/test.js"
  },
  "dependencies": {
    "@mcp-servers/shared": "*",
    "@modelcontextprotocol/sdk": "^1.17.3",
    "axios": "^1.11.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "nodemon": "^3.1.10",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.2"
  }
}
